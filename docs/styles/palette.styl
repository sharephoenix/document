
.page {
  position: relative;
  display: block;
}

.page-edit {
  position: relative;
}

.content {
  margin: 0;
  max-width: 100% !important;
  padding: 0px !important;
  p {
    padding: 0px;
  }
}

.sidebar {
  width: 294px;
}